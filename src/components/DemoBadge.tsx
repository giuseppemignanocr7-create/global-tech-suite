"use client";

export default function DemoBadge() {
  return (
    <span className="fixed top-4 right-4 z-50 bg-yellow-500 text-black text-xs font-bold px-3 py-1 rounded-full shadow-lg animate-pulse-slow">
      DEMO
    </span>
  );
}
